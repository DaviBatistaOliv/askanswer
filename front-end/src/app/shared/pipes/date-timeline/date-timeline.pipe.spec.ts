import { DateTimelinePipe } from './date-timeline.pipe';

describe('DateTimelinePipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimelinePipe();
    expect(pipe).toBeTruthy();
  });
});
